def garden_operations(num1=None, num2=None, file_path=None, key=None) -> tuple:
    if num1 is not None:
        num1_int: int = int(num1)
        if num2 is not None:
            zero_test: int = num1_int / 0
    if file_path is not None:
        file_content = open(file_path, "r")
        file_content = close()
    if key is not None:

    return num1_int, zero_test